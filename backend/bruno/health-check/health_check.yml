info:
  name: health_check
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/health-check"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
