info:
  name: health_check_db
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_url}}/health-check/db"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
