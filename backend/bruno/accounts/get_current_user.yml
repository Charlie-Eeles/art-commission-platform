info:
  name: get_current_user
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/accounts/users/me"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
