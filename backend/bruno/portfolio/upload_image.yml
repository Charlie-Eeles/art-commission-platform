info:
  name: upload_image
  type: http
  seq: 1

http:
  method: POST
  url: "{{base_url}}/portfolio/images"
  body:
    type: multipart-form
    data:
      - name: art_name
        type: text
        value: testing
      - name: image
        type: file
        value:
          - /Users/ce/Downloads/akira_poster.jpg
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
