info:
  name: get_all_tags
  type: http
  seq: 7

http:
  method: GET
  url: "{{base_url}}/portfolio/all-tags"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
