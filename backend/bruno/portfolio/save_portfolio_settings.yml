info:
  name: save_portfolio_settings
  type: http
  seq: 6

http:
  method: PUT
  url: "{{base_url}}/portfolio/settings"
  body:
    type: json
    data: |-
      {
        "description": "Tester",
        "is_public": true,
        "commission_slots": 5
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
