info:
  name: get_all_portfolio_images
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}/portfolio/images"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
