info:
  name: get_portfolio_settings
  type: http
  seq: 5

http:
  method: GET
  url: "{{base_url}}/portfolio/settings"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
